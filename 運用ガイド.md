# 同じ読みの かん字クイズ ― プロジェクト＆運用ガイド

このドキュメントは、アプリの**公開・更新・運用**と、**ここまでの作業の流れ**をまとめたものです。
（アプリ内部の仕様・データ構造の詳細は `README.md` を参照）

---

## 1. これは何か

小学1・2年生の漢字の「同じ読み／同音異義語」を、**例文で意味を理解して正しく使い分ける**ことを学ぶ、単一HTMLのクイズアプリ。スマホ・タブレット・PCのブラウザで動作。

- 問題数: **約163問**（使い分け 33問 ＋ 同じ読み 130問）
- 漢字DB: **164字**（1年生80字＋2年生で使う字）

---

## 2. 公開情報（これだけ覚えればOK）

| 項目 | 値 |
|---|---|
| **遊ぶURL** | https://mikushiba.github.io/kanji-quiz/ |
| リポジトリ | https://github.com/mikushiba/kanji-quiz |
| 本体（編集元） | `~/kanji-quiz/index.html` |
| 公開のしくみ | GitHub Pages（`main` ブランチのルートを自動公開） |

### スマホ／タブレットで遊ぶ
1. 上記URLをブラウザで開く（LINE・メールでURLを共有してもOK）。
2. アプリ風に使うなら「ホーム画面に追加」：
   - iPhone/iPad（Safari）: 共有ボタン □↑ →「ホーム画面に追加」
   - Android（Chrome）: 右上 ⋮ →「ホーム画面に追加」

---

## 3. ファイル構成（一本化後）

```
~/kanji-quiz/                 ← 唯一の本体フォルダ（Git管理・GitHub連携）
├── index.html               ← アプリ本体（これを編集する）
├── README.md                ← 仕様ドキュメント（画面・データ構造・問題の増やし方）
├── 運用ガイド.md             ← このファイル（公開・更新・履歴）
├── deploy.sh                ← 更新を1コマンドで公開反映
└── .gitignore

Google ドライブ family/
└── かん字クイズ_アプリ情報.md  ← 「本体は ~/kanji-quiz にある」と示すメモのみ
```

> 以前 Drive にあった `kanji-quiz.html` / `kanji-quiz-README.md` は、二重管理を避けるため削除し `~/kanji-quiz/` に一本化した（内容は GitHub と Drive ゴミ箱に保持）。

---

## 4. 更新のしかた（公開サイトへ反映）

`~/kanji-quiz/index.html` を編集したあと、次のどちらか。反映は1〜2分後。

```bash
# かんたん（用意したスクリプト）
~/kanji-quiz/deploy.sh "問題を追加"

# または手動
cd ~/kanji-quiz
git add -A && git commit -m "問題を追加" && git push
```

> 今後の修正依頼（問題追加・文言変更など）は、`~/kanji-quiz/index.html` を直接編集して push する運用。Drive側を触る必要はない。

---

## 5. これまでの作業の流れ（セッション履歴）

1. **企画**: 小2漢字の同音異義語クイズのアイデア出し。配当漢字（1年80字／2年160字）を確認。
2. **試作**: 単一HTMLでクイズを作成。「使い分け」「同じ読み」の2モード、絵文字フィードバック・星評価。
3. **タイトル/表記調整**: タイトルを「同じ読みの かん字クイズ」に、サブを「― 文を読んで、正しいかん字をえらぼう ―」に。ふりがな（ruby）撤去。
4. **学年選択＋DB化**: 学年セレクター（1年/2年/1・2年）を追加。漢字情報を `KANJI` データベース（学年・画数・音訓）に集約し、出題を自動フィルタ。
5. **操作性**: 🏠 HOME ボタン、← 前へ ボタンを追加。回答状態を問題ごとに保持（戻っても二重採点しない）。出題順・選択肢をランダム化。
6. **問題拡充**: 同じ読みグループを多数追加し約163問に。使い分けも会う/合う・明ける/空ける・丸い/円いで33問へ（1・2年の自然な上限）。
7. **公開**: GitHub Pages で公開（手順は第6章）。
8. **整理**: 本体を `~/kanji-quiz/index.html` に一本化、`deploy.sh`・ドキュメント整備、Drive重複を削除。
9. **強化 第1弾**: 出題数選択（10/20/ぜんぶ）、まちがい復習（結果に誤答一覧＋「まちがえた問題だけ もう一回」）、解説に「完成した文」と「漢字メモ（画数・音訓）」を追加、空欄のaria・選択肢順の固定・キーボード操作（数字/Enter/←→）・フォーカス可視化。`validate.mjs`＋GitHub Actions で問題データを自動検証。（※音声読み上げは検討の結果不採用）
10. **強化 第2弾（ゲーム性・継続）**: localStorage によるセーブ層を追加。
    - **漢字ずかん**：正解した漢字がカード（画数・音訓付き）として図鑑に増える。1回正解で図鑑入り→累計3回で★マスター。図鑑対象は「出題の正解になる漢字」のみ（全112字）。
    - **連続正解コンボ**：れんぞく正解でコンボ表示＋紙ふぶき演出。
    - **メダル**：8種（はじめの一歩／全問正解／コンボ5・10／ずかん10・50字／マスター10字／ずかんコンプリート）。達成で自動解放。
    - ホームに「📖 かん字ずかん」「🏅 メダル」入口、結果画面に最高コンボ・新規メダル表示。
11. **強化 第3弾（メダル拡張・デイリー・復習）**:
    - **メダルを20種に拡張**（あそぶ回数／全問正解回数／累計正解数100・500／コンボ5・10・20／ずかん10・50・100・コンプ／マスター10・50・全／まちがいなおし／デイリー3日・7日連続）。
    - **デイリーチャレンジ**：日付をタネにした決定的シャッフルで「その日は全員・何回でも同じ10問」。クリアで**連続日数（ストリーク）**を加算（ホームに🔥表示）。
    - **まちがい復習（横断）**：まちがえた問題を localStorage に蓄積し、ホームの「🔁 まちがい ふくしゅう」からいつでも再挑戦。正解すると復習リストから自動で消える（克服＝comeback）。
    - 追加のセーブ項目：totalCorrect / perfectCount / comebacks / wrong{} / daily{streak,bestStreak,lastDay}。問題IDは BANK の index。
12. **強化 第4弾（3年生の解放・記録の保持）**:
    - **3年生を追加（解放制）**：ふだんは「3年🔒」。**1・2年の漢字を図鑑コンプリート（112字）で解放**。解放後は学年セレクターで「3年」が選べ、デイリーや「ぜんぶ」にも3年生が混ざる。メダル「🔓 3年生かいきん」も追加。
    - 3年生コンテンツ初期セット：使い分け **開ける/空ける/明ける・速い/早い・帰る/返る・登る/上る・追う/負う**、同じ読み **ちゅう（注/柱/中）**。DBに3年生漢字（開・速・返・登・追・負・注・柱）を追加。
    - 学年判定を一般化（`gradeSpec`：'1''2''3''all'＝解放状況で1・2 or 1〜3）。`validate.mjs` は学年1〜6を許可。
    - **記録の保持（仕組み化）**：localストレージに加え、ホーム下部に「💾 きろくを ほぞん／📂 よみこむ」を追加（JSONバックアップ＝端末移動・キャッシュ消去対策）。セーブに `version` を持たせ将来の移行に備える。
13. **3年生「同じ読み」拡充（第1波）**: し/とう/こう/あん/ひ/び/はな/よう/やく/どう/ちょう/きょく/たい。問題数203・漢字DB203字。
14. **3年生「同じ読み」拡充（第2波）**: しん(深/進/身/神)・しゅう(拾/終/習/集)・しゅ(守/取/酒)・じ(持/次/事)・じゅう(住/重)・おん(温/音)・たん(炭/短)・はん(坂/板)・きゅう(急/宮)・しょ(暑/所)・み(味/実)・しょう(消/勝)・こ(湖/古)・しょく(植 追加)。問題数236・漢字DB235字。
    - 方針メモ: **既存の「同じ読み」グループは基本的に削除しない**（追加・拡充のみ）。
15. **3年生 同じ読み 第3波＋あそびかた画面**: けつ/らく/りょく/ほう/ひょう/く/た/さい/てん/もの/てい/いん/おう/かん、既存グループに 起(き)・送(そう)・陽(よう)・岸(がん) を追加、使い分け **きる(切る/着る)** も追加。問題数261・漢字DB270字。
    - **子ども向け「❓ あそびかた」画面**を追加（ホーム上部のボタンから。やさしい日本語で操作・図鑑・メダル・デイリー・復習・バックアップを説明）。
16. **ドキュメント刷新＋Lark同期の準備**: `README.md` を現行（261問/270字・ゲーム性・解放・バックアップ・あそびかた）に全面刷新。Lark へのデータ同期手段を整備（下記「Lark Base 同期」）。CSV書き出し（`export/kanji.csv`・`export/questions.csv`）＋ API自動同期スクリプト `lark-sync.mjs` を追加。
17. **答え漏れバグ修正＋3年生 第4波**: 例文の「（…）」内の熟語に答えの漢字が出てしまう問題を10件修正（電車・国語・東京・会社・地図・来週・午前・元気・地面・答え合わせ）。`validate.mjs` に**「答えの漢字が例文に出ていないか」自動チェックを追加**（再発防止）。3年生 同じ読みを追加（きょう=橋・しゃ=写・せき=昔・けい=係/軽・こん=根・めい=命・もん=問・ゆ=油・ゆう=遊・しん=真）＋使い分け **たま(玉/球)**。問題数274・漢字DB287字。
18. **メダル拡充(20→41)＋3年生 第5波**: メダルを **41種**に（あそぶ1/5/10/20/50/100・全問正解1/3/5/10・累計100/200/500/1000・コンボ5/10/15/20/30・なおし10/25/50・ずかん10/25/50/75/100/150/200/コンプ・マスター10/25/50/75/100/全・デイリー3/7/14/30日・3年かいきん）。3年生 同じ読み追加（えい=泳・や=屋・か=荷・はい=配・しゅ=主）。問題数279・漢字DB295字。
19. **メダル100種＋3年生 仕上げ**: メダルを **100種**に拡充（プレイ1〜1000・全問正解1〜50・累計正解50〜5000・コンボ2〜50・なおし5〜100・ずかん1〜コンプ・学年別ずかんコンプ(1/2/3年)・マスター1〜全・グランドマスター・デイリー連続2〜100日・デイリーのべ1〜100）。新カウンタ `dailyTotal` と `dexGradeDone(g)` を追加。3年生 同じ読み（り=旅・と=都）も追加。問題数281・漢字DB298字。
20. **漢字DBを全配当字に補完**: 1〜3年の **学年別漢字配当表 全字** を収録（2年+67字・3年+75字）。**1年80/80・2年160/160・3年200/200＝計440字**。配当照合で不足0・学年ズレ0を確認。出題未使用の字も画数・音訓付きで収録（教育漢字＝常用漢字の一部）。CSV書き出しも440字に更新。
21. **DB拡充にともなう問題拡充（1・2年）**: 全配当字がそろい、これまで選択肢が足りず作れなかったグループを追加（+30問）。1年＝せき(夕/石/赤)・けん(犬/見)・ちゅう(中/虫)・もく(目/木)・きん(金)・ちょう(町)。2年＝き(黄)・し(市/止/思/紙/姉)・せつ(雪/切)・そう(走)・ちゅう(昼)・た(多)・めい(鳴)・わ(話)・か(何)・かい(絵)・かく(角)・さい(細)・ぜん(前)・しゅ(首)・しょ(書)。問題数311。
    - 方針メモ: **常用漢字をDB追加するときは毎回 同じルール**（配当準拠の `{g,s,on,kun}`・正確な画数/音訓・答え漏れ無し・同じ読みグループ保持）で行う。
22. **別の音読みで作るグループを追加（+21問）**: 漢字の別読みで成立するグループ（えん=円/園/遠・そく=足/速/息・なん=男/南・うん=雲/運・じょう=乗・ず=頭・ふ=風・じょ=女・じん=人・りゅう=立・ろく=六・げ=外・がく=学・さ=左・だい=弟・へい=平）。「1字に複数の読み」も学べる。問題数331・漢字DB440字。
23. **運用の道具化**: 毎回その場書きだった処理を常設ツールに（リポジトリ内 `tools/`）。`tools/db-coverage.mjs`（配当網羅チェック）・`tools/onaji-candidates.mjs`（作れるグループ候補）・`tools/export-csv.mjs`（CSV書き出し）を追加。
24. **更新フローのskill化（最終構成）＋公開方針の確定**:
    - 更新の標準フロー（編集→検証→公開→記録）を **skill `kanji`** に定義。`/kanji` で発動。
    - 配置は **利用フォルダ専用**＝Google ドライブの作業フォルダ `共有ドライブ/family/.claude/skills/kanji/SKILL.md`。**グローバルにもリポジトリにも置かない**（このフォルダで起動したセッションだけで有効）。
    - skillはアプリ本体（`~/kanji-quiz`）の外にあるため、実体の手順は `cd ~/kanji-quiz` してから実行する内容で記述。
    - **公開方針**: GCP（Firebase等）移行は検討の結果見送り、**GitHub Pages（公開リポジトリ）のまま**運用する。
25. **効果音＋3年生 解放ヒント（家族フィードバック対応）**:
    - **効果音を追加（Web Audio・音声ファイル不要／オフラインでも鳴る）**：`playSound()` を `choose()`（正解/不正解）と `confettiBurst()`（クラッカー）に組み込み。音はすべてコードで合成（オシレーター＋ノイズ）。
      - **正解＝ドアベル風「ピン♪ ポーン♪」**：E6(1318Hz)→C6(1047Hz) の2音に倍音を重ね、残響を長めにして鐘らしく。
      - **不正解＝番組風の濁ったブザー「ブッ ブー」**：半音差の2音（233+220Hz→220+207Hz）を sawtooth で重ね、わざと不協和に。2連で鳴らす。
      - **クラッカー＝「パン！」＋キラキラ**：破裂ノイズ（バッファ）＋上昇する3音。コンボ／全問正解／解放など紙ふぶきが出る場面で共通。
      - ※家族フィードバックで音色を2回微調整（正解の鐘・不正解のブザー）。`tone(freq,start,dur,type,vol)` ヘルパで各音を定義しているので、周波数・長さ・波形を変えれば調整可能。
    - **音声 ON/OFF 切替**：ホーム右上に「🔊 おと ON／🔇 おと OFF」トグル。設定は `store.soundOn` で localStorage に保存。OFF中は全効果音を抑止。
    - **3年生まで あと何字を表示**：ホームの学年セレクター下に「🔒 3年生まで あと N字（have/total）」を常時表示（解放後は「✅ 3年生も あそべるよ！」）。ロック中に「3年」を押したときの注意文にも残り字数を表示。
26. **称号（あいぼう）システム＋ホーム上部ボタン整理（家族フィードバック対応）**:
    - **称号＝あいぼうを1本化**：当初コイン→相棒進化＋メダル→称号の2系統で作ったが、フィードバックで「卵（あいぼう）と称号は1つでよい／Lv表記不要／"あと何問正解で次の称号"にしたい」となり、**累計正解数（`store.totalCorrect`）で育つ単一の称号システムに統合**。コイン・上部の称号バッジ・Lv表記は廃止。
    - **段階（`STAGES`）**：全17段階。🥚みならい→🐣かけだし→🐥がんばりや→🐦じょうず→🦜もの知り→🦉はかせ→🦅せんせい→🐺名人→🦁達人→🐲マスター→🐉グランドマスター→👑キング→🌟チャンピオン→☄️レジェンド→🧙仙人→🐉✨神さま→🌌うちゅういち。しきい値（累計正解数）0/30/80/150/300/500/800/1200/1800/2600/3800/5500/8000/11500/16000/22000/30000。`rankStage(n)` で判定。**1〜6年生まで拡充する前提で高め・多段**（キングの上も用意。最上位3万問は重課金級のやり込み想定）。しきい値は配当拡充の進み具合に合わせて調整可。
    - **表示**：ホーム上部のパネル（`.buddy`）にアイコン＋称号名＋「これまで せいかい N問」＋進捗バー＋「**あと N問 正解で「次の称号」**」。`renderRank()` を `refreshHome()` から呼ぶ。結果画面では称号が上がった回にクラッカー＋「✨称号アップ！」（`showResult` 内で当回ぶんを差し引いた before/after を比較）。
    - **ホーム上部のボタン整理**：大きな「あそびかた」バナーを廃止し、**「❓あそびかた」「🔊おと」を横並びのコンパクトボタン**に（`.topRow`/`.topBtn`）。
    - 称号は `totalCorrect` から導出するため専用のセーブ項目は不要（既存バックアップでそのまま反映）。
27. **称号を17段階に拡張（1〜6年生 拡充を見込んだ高め設定）**：累計正解数のしきい値を 0/30/80/150/300/500/800/1200/1800/2600/3800/5500/8000/11500/16000/22000/30000 に。キングの上に チャンピオン→レジェンド→仙人→神さま→🌌うちゅういち を追加。配当拡充の進み具合に合わせて調整する想定。
28. **4・5・6年生の枠を新設（DBは空のまま）**：
    - 学年ボタンを 1〜6年＋ぜんぶ に拡張し、`#seg` を**4列グリッド**で折り返し表示。
    - 解放ロジックを3年専用から**3〜6年に一般化**（`MAXGRADE=6`／`gradeUnlocked(g)`／`maxUnlockedGrade()`／`gradeHasContent(g)`／`collectiblesUpTo(g)`）。**前の学年までを図鑑コンプリートで順番に解放**。解放フラグは `store.unlocked3..6`。`checkUnlock()` は新規解放した学年番号を返す（結果バナーも「N年生が かいきん！」に対応）。
    - **問題がまだ無い学年（4〜6年）は「じゅんびちゅう」**として常にロック表示。タップすると「じゅんびちゅう」案内。`gradeSpec()`・`startDaily()`・`ぜんぶ` は解放済み学年のみ対象。
    - **後から `KANJI`/`BANK` に各学年を足すだけ**で、その学年は自動的に解放チェーン（前学年コンプリートで解放）に組み込まれる。`grade3Unlocked()` は `gradeUnlocked(3)` の別名として互換維持。
29. **3年生解放のデッドロック修正（家族フィードバック「あと20字から進捗が動かない／図鑑が埋まらない」）**：原因は、答えが1〜2年でも**選択肢に3年の字が混ざる問題**は「習っていない字は出さない」ルールで出題されず、その20字（音古楽力方玉昼多話前首書足雲頭風女人立六）が永久に集められず解放が詰んでいたこと。解放条件を「全COLLECTIBLE」ではなく**実際に集められる字だけ**で判定するヘルパー `collectableUpTo(g)`（選択肢が全てg学年以下の問題に出る正解漢字）を追加し、`gradeUnlocked()`・図鑑/ホームの「あと○字」表示・ロック時アラートをこれに差し替え。3年解放の必要数は143字に。解放後は「ぜんぶ」モードで残り20字も回収可能（解析で確認済み）。問題データは無改変・ロジックのみ。詳細な調査記録は `docs/2026-06-25-3年生解放デッドロック.md`。
30. **4年生の漢字DBを拡充（配当表どおり全202字）**：学年別漢字配当表（2017告示・2020全面実施）の4年配当202字をWikipedia等で網羅確認し、`KANJI` に `{g:4, s:画数, on:[音], kun:[訓]}` で全字追加（茨・媛・岡・潟・熊・鹿・栃・梨・崎・阪・阜など県名漢字を含む）。`tools/db-coverage.mjs` の `HAITOU` に4年リストを追記し、**4年: 202/202 完全**を確認。`validate.mjs` パス（KANJI 642字）。出題（BANK）は未追加で、4年生は引き続き「じゅんびちゅう」。
31. **4年生の「同じ読み」問題を一気に追加（183問）**：4年配当202字のうち、学年1〜4の字だけで「同じ読み」グループが組める **183字** を 答え に採用し、読み別グループ（あ〜ろ）で出題を作成（`BANK` 同じ読み 281→465）。すべて選択肢を **学年4以下** にそろえ（図鑑で集められる＝解放チェーンが詰まらない条件）、例文に答えの漢字を出さないルールを順守。`validate.mjs` 全パス（問題 515 / KANJI 642）。**残り19字（茨媛岡潟議熊芸材残順卒達栃梨末民浴陸類）は、学年1〜4の範囲に「同じ読み」の相手がいない**ため未出題（DBには収録済み）。今後 別形式（意味から選ぶ／5年生解禁後に相手字とペア）で回収予定。県名漢字は県名の読みで出題（鹿=鹿児島/佐=佐賀/埼=埼玉/崎=宮崎/滋=滋賀/縄=縄文/奈=奈良/阪=大阪/阜=岐阜/賀=年賀）。
32. **（一度 `imi` モードで19字回収→撤回）**：当初、同じ読みの相手がいない19字を新タイプ `imi`（いみでえらぶ）で出題したが、家族の判断で撤回。「同じ読み(読み→漢字)」では相手のいない字が出せず図鑑コンプリートできない問題は、次の **読みクイズ** で解決した。
33. **読みクイズ（type:'yomi'）を自動生成モードとして追加＝全漢字を図鑑で集められる**：
    - **③読みクイズ**モードを新設（モードは ①使い分け ②同じ読み **③読みクイズ** ④ぜんぶ の4つ）。漢字を見せて**音読み・訓読みをえらぶ**（「同じ読み」の逆向き）。
    - **`KANJI` データから自動生成**（`makeYomiQ`/`buildYomiPool`/`yomiReadings`/`kataToHira`）。答え＝その字の読みの一つ、ダミー＝他字の読み（その字の読みと重複しないもの）。**手作り問題ゼロ・全学年(1〜4年)自動対応**＝低学年で空っぽにならない。新しい漢字を足せば読みクイズも自動で増える。
    - 図鑑(`COLLECTIBLE`)を **BANK由来 → 配当の全字（DB全642字）** に拡張。読みクイズで全字集められるので、孤立19字も含め**図鑑コンプリート可能**。未収集カードは「？ みつけよう」表示。
    - **解放ロジックは無改変**（`collectableUpTo` は BANK由来のまま）。よって既存の進行・「あと○字」表示・既解放フラグに影響なし＝**デッドロックも再ロックも無し**。読みクイズで集めた字も `store.kanji` に入るので解放条件にもそのまま効く。
    - 実装の分岐：`render`/`choose`/`showFeedback`/結果のまちがい一覧 で `type==='yomi'` を分岐（大きな漢字表示・読み選択肢・正解で `q.kanji` を図鑑記録・自動生成なので復習リストには入れない）。`'all'` モードは BANK＋読みクイズを混ぜる。
    - 実機(Chrome)スモーク確認：③読みクイズが表示・採点・図鑑記録され、図鑑 1/**642**字、コンソールエラー0。問題データ `validate.mjs` 全パス（BANK 515：使い分け50/同じ読み465）。
    - **詳細な作業記録: `docs/2026-06-25-4年生追加と読みクイズ.md`**（4年生DB拡充→同じ読み183字→imi撤回→読みクイズ採用までの全経緯・設計・デッドロック非該当の理由・検証）。
34. **全学年に「同じ読み」「使い分け」を追加（+29問）**：1〜4年の字だけで まだ作れる未作成グループを追加。同じ読み8グループ（みょう/ごう/ふう/じつ/け/じき/びょう/ぎょう＝別の音よみで漢字の複数読みも学べる）＋使い分け6グループ（あつい=暑/熱・はかる=計/量/図・なおす=治/直・かえる=変/代/帰・はやい=早/速・つく=付/着）。答えの学年が1〜4年に散るよう選定。選択肢は全部 学年4以下。読みが不自然に難しい候補（悪=お/会=え 等）は子ども向けに除外。`validate.mjs` 全パス（**BANK 544：使い分け63/同じ読み481**）。デッドロック・シミュレーション再実行で `collectableUpTo(2/3/4)` 全字が集められること（解放詰まり無し）を確認。
35. **5年生の漢字DBを拡充（新配当どおり全193字）＋問題追加（+31問）／5年モード解放**：学年別漢字配当表（2017告示・2020全面実施）の**新**5年配当193字を公式リストで照合し `KANJI` に `{g:5, s:画数, on:[音], kun:[訓]}` で全字追加。新配当で4年へ移った県名系（賀・群・徳・富）や6年へ移った字（恩・券・承・退・敵・預 等）を取り違えないよう確認。`tools/db-coverage.mjs` の `HAITOU` に5年を追記し **5年: 193/193 完全**。問題は **同じ読み8グループ**（こう=効厚耕構興／さい=再災妻際／げん=限現減／ひ=費比肥／む=務夢／ほう=報豊／ぼう=防暴／き=寄喜）＋**使い分け5グループ**（つとめる=務/努・うつる&うつす=移/写・おさめる=修/治・たつ=断・はかる=測〔測/計/量〕）を追加。選択肢は全部 学年5以下。`validate.mjs` 全パス（**BANK 575：使い分け71/同じ読み504**、KANJI 835字）。**DB追加で `gradeHasContent(5)` が真となり5年が「じゅんびちゅう」から解放対象に**（解放条件は従来どおり`collectableUpTo(4)=461字`の収集・ロジック無改変）。読みクイズ・図鑑も5年193字へ自動拡張。シミュレーションで 5年モードに出るBANK問題28問、読みクイズ5年193字、`collectableUpTo(5)=488字`が全て到達可能（6年解禁も詰まらない）・例文への答え漏れ無しを確認。
36. **5年×低学年の同音字グループを追加（+20問）**：5年がDBに入ったことで作れるようになった「5年の字×なじみのある低学年の同音字」を追加。**同じ読み17**（にん=任/人・だん=断/男・ちく=築/竹・りつ=率/立・ふん=粉/分・どく=独/読・しき=織/色・ほ=保/歩・ぼ=墓/母・ぶつ=仏/物・は=破/波・じゅ=授/受・ひん=貧/品・めん=綿/面・る=留/流・ざい=罪/財/在・ぞう=増/像/造）＋**使い分け3**（あつい=厚〔既存の暑/熱に5年答えを追加〕・たつ=絶つ〔既存の断つ/立つに追加〕・つくる=造る/作る）。**設計の肝：答えを必ず5年の字に限定**（選択肢に5年字が入るので下学年モードからは自動除外）→ 5年モード・ぜんぶモードだけが充実し、**1〜4年の解放チェーンは不変**（`collectableUpTo(2/3/4)=144/277/461` 5年追加前と完全一致・下学年モードへの5年混入0件を再検証）。低学年の同音字を相方にするので5年の子の復習にもなる。`validate.mjs` 全パス（**BANK 595：使い分け74/同じ読み521**、KANJI 835字）。5年モードに出るBANK問題は28→**48問**に。
   - 注：1年など低学年モード“そのもの”は5年字では増やせない（「習っていない字は出さない」ルールで弾かれる＝意図どおり）。増やせるのは「5年が答え」のグループのみ。
37. **6年生の漢字DBを拡充（新配当どおり全191字）＋問題追加（+22問）／6年モード解放＝全学年そろう**：新配当6年191字を公式リストで照合し `KANJI` に `{g:6, s, on, kun}` で全字追加（株・届は音なし＝訓のみ等も正確に）。`tools/db-coverage.mjs` に6年を追記し **6年: 191/191 完全**、全学年そろって **KANJI 1026字（80+160+200+202+193+191）**。問題は5年と同じ設計（答えを6年字に限定し選択肢は学年6以下）で **同じ読み17**（すい=推/垂・ぎ=疑・ぜつ=舌・らん=乱/卵・つう=痛・にゅう=乳・よく=欲・はい=拝/背・せん=洗/染/泉・たん=探/担・こう=降）＋**使い分け5**（おさめる=収める/納める〔既存の修/治に追加〕・うつる=映る〔移/写に追加〕・つとめる=勤める〔務/努に追加〕・そなえる=供える/備える）。`validate.mjs` 全パス（**BANK 617：使い分け79/同じ読み538**）。**下学年の解放チェーンは不変**（`collectableUpTo(2/3/4/5)=144/277/461/506`、下学年モードへの上位学年答えの混入0件を再検証）。6年モードに出るBANK問題22問・読みクイズ6年191字・`collectableUpTo(6)=528字`すべて到達可能・例文への答え漏れ無しを確認。これで**1〜6年すべて配当どおりDB化＋全モード解放**。
38. **6年×低学年の同音字グループを追加（+23問）**：5年と同じ手法で「6年の字×なじみのある低学年の同音字」を追加。**同じ読み21**（い=異/医・かい=灰/海・き=机/記・きゅう=吸/球・きょう=胸/橋・きん=筋/金・けつ=穴/血・けん=絹/県・げん=源/原・こん=困/根・さ=砂/差・じゅう=縦/住・しん=針/森・じょう=蒸/場・そう=窓/送・ぞう=蔵/増・ふく=腹/服・ほ=補/保・ほう=宝/報・やく=訳/薬・り=裏/里）＋**使い分け2**（しお=潮/塩・あたたかい=暖/温）。**答えを6年字に限定**＝下学年モードからは自動除外され、`collectableUpTo(2/3/4/5)=144/277/461/506` は5年追加前から不変・混入0件を再検証。`validate.mjs` 全パス（**BANK 640：使い分け81/同じ読み559**）。6年モードに出るBANK問題は22→**45問**に。`collectableUpTo(6)=551字` 到達可能・例文への答え漏れ無し。
39. **同訓異字（同じ訓で使い分け）を全学年に追加（+26問）**：答えの学年が1〜6年に散る12グループ＝あらわす（表/現/著）・おる（折/織）・きく（聞/効/利）・なく（鳴/泣）・のぼる（上/登）・わかれる（分/別）・うつ（打/討）・さめる（冷/覚）・すむ（住/済）・はじめ（初/始）・まじる（交/混）・さす（指/差）。これまでの「高学年が答え×低学年の同音字」と違い、**低学年が答えの使い分けも増える**ので各学年の使い分けモードが充実。安全性：これらは選択肢に必ず別学年の同訓字が入るため、**`collectableUpTo(2/3/4)=144/277/461` は完全に不変**（5→508・6→557に微増したぶんも全て到達可能・到達不能ゼロ・混入0件を再検証）。`validate.mjs` 全パス（**BANK 666：使い分け107/同じ読み559**）。
40. **同訓異字 第2弾を追加（+26問）**：やさしい（易/優）・へる（減/経）・やぶれる（破/敗）・うむ（生/産）・おう（追/負）・のぞむ（望/臨）・かう（買/飼）・きる（切/着）・たてる（立/建）・とまる（止/留）・はなす（話/放）・さげる（下/提）・とる（取/採）の13グループ。引き続き答えが全学年に分散。`collectableUpTo(2/3/4)=144/277/461` 不変（5→512・6→564は到達可能）・混入0件・例文への答え漏れ無しを再検証。`validate.mjs` 全パス（**BANK 692：使い分け133/同じ読み559**）。これで同訓異字（同じ訓の使い分け）はおもなものをほぼ網羅。
41. **5・6年漢字どうしの「同じ読み」を追加（+20問）**：高学年に多い同音字を、訓のある字を答えにして6グループ＝しょう（招/傷/障）・せい（勢/盛/誠）・そう（奏/装/操）・かん（幹/慣/巻/干）・し（支/枝/示/至/私）・き（危/貴）。選択肢の相手は同音の5・6年字（証/賞/将/制/精/製/聖/総/創/層/刊/看/簡/史/士/師/資/視/詞/誌/机/規/揮 など）。答えは全て5・6年なので**下学年解放に非干渉**（`collectableUpTo(4)=461` 不変・5→514/6→584は到達可能・混入0件）。`validate.mjs` 全パス（**BANK 712：使い分け133/同じ読み579**）。6年モードに出るBANK問題は63問に。
42. **アーキテクチャ：漢字DBを共有モジュール化（Phase 1）**：将来 別アプリ（送り仮名の練習 等）でも同じ漢字DBとUI/UXを使えるよう、`KANJI`（と基本ヘルパー `wordGrade`）を **`shared/kanji-db.js`（唯一の正データ）** に分離。**1か所直せば、参照する全アプリ＋全ツールに同時反映**される設計。
   - 方式：file://（ダブルクリック直開き）でも動くよう、**グローバル代入のクラシック`<script>`**で共有（ESモジュール/fetchは file:// で不可のため不採用）。`index.html` は `<script src="shared/kanji-db.js?v=…"></script>` を先に読み、本体スクリプトはグローバル `const KANJI`/`wordGrade` を参照（中身のコピーは持たない）。`shared/kanji-db.js` は `window.KANJI` と Node の `module.exports` 両対応。
   - ツール：`validate.mjs`/`tools/db-coverage.mjs`/`onaji-candidates.mjs`/`export-csv.mjs` を共通ローダ **`tools/lib.mjs`** 経由に統一（KANJIは共有モジュール、BANKは`index.html`から）。従来の「index.htmlを正規表現＋vmで切り出す」ハックを廃止。`validate.mjs` の「KANJI形チェック」が**全アプリ共通のスキーマ門番**になる。
   - キャッシュ反映：`deploy.sh` が **`shared/kanji-db.js` の変更を検知したときだけ** 全`index.html`の `?v=` を自動更新し、公開後のキャッシュ更新を保証。
   - 検証：実ブラウザ（Chrome・file://直開き）で**コンソールエラー0**、`KANJI`1026字/`wordGrade`/図鑑`COLLECTIBLE`1026/出題プール生成すべて正常。`validate`/`db-coverage`/`export-csv` も移行前と同値（**BANK 712・KANJI 1026・配当完全**）。挙動ゼロ変化。
   - 送り仮名アプリは `kun` の送りがな符号化（例 `'うつ(る)'`）から自動生成できるため、DB修正が問題にも自動反映される。次フェーズ：`shared/kanji-ui.css`（見た目共有）→ `okurigana/index.html`（新アプリ）。
43. **Phase 2：見た目を共有CSS化**：`index.html` の `<style>` を **`shared/kanji-ui.css`（全アプリ共通のデザイントークン＋UI部品）** に抽出し、`<link rel="stylesheet" href="shared/kanji-ui.css?v=…">` に置換（`<link>`は file:// でも読めるので直開きOK）。実ブラウザで body背景 `#fff7ed`・`.card`角丸22px・`--accent` 変数・エラー0 を確認、見た目は完全維持。
44. **Phase 3：送りがな練習アプリを新規作成（`okurigana/index.html`、公開 `/okurigana/`）**：共有DB＋共有CSSの上に、**`parseOkurigana` で `kun` から自動生成**する送りがなクイズを実装。出題＝大きな漢字＋読みを見せ、送りがなの「長さ違い」の選択肢から正しい1つを選ぶ（例 `移`「うつる」→ `移る / 移つる / 移`）。学年セレクタ（1〜6／ぜんぶ）＋10問＋採点＋漢字メモ。**送りがな語 673語**を自動抽出（学年別 36/95/127/147/126/142）。実ブラウザ（file://直開き）で出題・採点・フィードバック・画面遷移すべて正常、コンソールエラー0、見た目はクイズと統一。**DBを直すと送りがなアプリの問題も自動で直る**（派生データを持たない設計）。
    - `deploy.sh` を一般化：`shared/` 配下（kanji-db.js／kanji-ui.css 等）に変更があれば 全 `index.html` の `?v=` を一括更新。
    - これで「**1つの漢字DB＋1つのUI/UXを、複数アプリで共有**」というご要望のアーキテクチャが完成。今後アプリを足すときも `shared/` を参照するだけ。
45. **サイト構成をポータル化：ルートを「漢字クイズ」トップに、各クイズを並列の子に**：これまで `/` が同じ読みクイズそのものだったのを、**`/` を入口メニュー（ポータル）**にし、各クイズを一段下げて並列配置。
    - 構成：`/index.html`（トップ＝メニュー）／`onaji/index.html`（同じ読み＝旧 `/index.html` を `git mv`）／`okurigana/index.html`（送りがな）／`shared/`（共有DB・CSS）。公開URLは `…/`（トップ）・`…/onaji/`・`…/okurigana/`。
    - トップは共有CSSの `.mode-btn` を再利用したメニュー（📖同じ読み／✏️送りがな＋「これからふえます」）。各クイズ上部に「← 漢字クイズ トップへ」（`.toplink`）、各子の最下部に「← 漢字クイズ トップへ」（`.applink`）を共有CSS化。
    - 影響：**BANKは `onaji/index.html` に移動**したので `tools/lib.mjs` の読み取り先を `onaji/index.html` に変更（KANJIは引き続き `shared/kanji-db.js`）。`onaji/index.html` の共有参照は `../shared/…` に修正。`deploy.sh` の `find -name index.html` は3つ（トップ・onaji・okurigana）すべての `?v=` を更新。
    - 検証：実ブラウザ（file://直開き）でトップ→各クイズの遷移・往復リンク・共有DB/CSS読込・コンソールエラー0を確認。`validate`（BANKをonaji/から取得）/`db-coverage` とも同値（BANK 712・KANJI 1026・配当完全）。
    - 注意：旧ブックマーク/ホーム画面追加（`…/kanji-quiz/`）は**トップ（メニュー）**を開くようになる（同じ読みは1タップ先の `…/onaji/`）。
46. **送りがなアプリを onaji と同じ構成・遊びフローに（育成系は除く）**：家族の要望で `okurigana/index.html` を onaji と同じUI/UXに作り替え。揃えた範囲＝**ホーム上部（❓あそびかた／🔊おと ON〔Web Audioの正誤音〕）・問題数えらび（10/20/ぜんぶ）・進捗バー＋🔥コンボ＋スコア・前へ/HOME・⭐結果＋コメント＋最高コンボ＋まちがい一覧＋「まちがえた問題だけ もう一回」**。共有CSSの部品（`.topRow/.seg/.bigkanji/.choice/.feedback/.stars/.comboBest/.review/.again` 等）をそのまま再利用。問題は引き続き `parseOkurigana` で自動生成（送りがな語673）。実ブラウザ（file://）でホーム→出題→コンボ→前へ復元→⭐結果→まちがいだけ再挑戦まで全フロー確認・コンソールエラー0。
    - **方針メモ（次段階）**：育成系のうち **漢字図鑑・称号・メダルは `shared/` の共通モジュールに置く**（家族合意）。学習者の進捗は1つで、onajiでもokuriganaでも正解すれば同じ図鑑・同じ称号・同じメダルにたまる＝アプリ横断で1つの育ち。Phase 4（`shared/quiz-core.js`＋共有セーブ層）でまとめて実装予定。デイリー/セーブUI等もそこで検討。
47. **育成系（図鑑・称号・メダル）を共有モジュール化＝アプリ横断で1つの進捗（Phase 4 第一弾）**：`shared/quiz-core.js`（`window.QuizCore`）を新設。中身＝進捗ストア（**localStorageキー `kanjiQuiz.v1`**）／図鑑(COLLECTIBLE・dexCount・masterCount)／recordCorrect・bumpCorrect・noteCombo・notePlay・notePerfect／**称号 STAGES(17段)・rankStage**／**メダル MEDALS(40)・checkMedals**／描画ヘルパー(renderBuddy・dexGridHTML・medalsGridHTML)。STAGES/MEDALSはonajiと同一定義。
    - **キモ：localStorageはオリジン単位**（`mikushiba.github.io` 全体で1つ）。共有モジュールがonajiと同じキー `kanjiQuiz.v1`・同じスキーマを使うので、**onajiを1行も触らずに進捗が自動合算**される。`store` は full-load→一部更新→full-save で既存フィールドを保持（onajiのdaily/wrong/unlocked等は壊さない）。BANK依存は持たない（unlock系はonaji側が担当）。
    - **okuriganaに組込み**：ホームに称号(buddy)＋📖図鑑/🏅メダルボタン、結果に獲得メダル/称号アップ表示、図鑑・メダル画面を追加。正解で `QuizCore.recordCorrect/bumpCorrect/noteCombo`、結果で `notePlay/notePerfect/checkMedals`。
    - **実証**：okuriganaで10問全問正解→`totalCorrect=10`/図鑑9字/メダル7獲得。その後 **onajiを開くと totalCorrect=10・図鑑9字・称号がそのまま反映**（cross-app合算をブラウザで確認）。実ブラウザでエラー0。これでokuriganaホームは onaji とほぼ同一（残りは デイリー/まちがい復習/セーブUI のみ。必要なら同様に共有化）。次段階：onaji本体も `shared/quiz-core.js` に寄せて重複を解消（Phase 4 第二弾）。
48. **送りがなに デイリー／まちがい復習／セーブ・読込 を追加（ホームが onaji とほぼ完全一致）**：
    - **📅 きょうの チャレンジ**：`todayIndex()`＋`mulberry32`シードの決定的シャッフルで「その日は何回でも同じ10問」。クリアで連続日数（`store.okuriDaily`）。実機で2回開始→同一10問・決定性を確認。
    - **🔁 まちがい ふくしゅう**：まちがえた送りがな語を `store.okuriWrong{語:true}` に蓄積、正解で克服（削除）。ホームに「のこり○問」。復習はその語だけ最大20問。実機で 追加→克服 を確認。
    - **💾 セーブ／📂 読込**：`QuizCore.exportSave()/importSave()`（共有モジュールに実装）。**共有ストア全体**を入出力するので、これで保存/読込すれば onaji 含む全進捗をまとめてバックアップ・移行できる。
    - 設計：図鑑/称号/メダル/累計は **共有（合算）**、デイリー連続日数と復習リストは **送りがな専用フィールド**（`store.okuriDaily`/`okuriWrong`）として同じ共有ストア内に名前空間分離（他アプリのフィールドは保持）。`validate`/CI 不変、実ブラウザでコンソールエラー0。**okuriganaホームは onaji とほぼ完全一致**（差は 学年の🔒ロック＝okuriganaは学年制限なし、と 出題ボタンが単一モードの2点のみ）。
49. **onaji本体も共有モジュールに寄せて重複コードを解消（Phase 4 第二弾＝完了）**：これまで onaji は育成系を自前実装・okuriganaは共有実装で**二重**だったのを、onaji も `shared/quiz-core.js` を読み込んで委譲する形に統一。
    - まず `QuizCore.MEDALS` を onaji 全件（**100種**）に拡張（superset化。`unlock3` の判定は BANK非依存の `!!store.unlocked3` に）。STAGES(17段)は元から同一。
    - onaji 側を置換：`store`/`saveStore`→`QuizCore.store`/`QuizCore.save`、`COLLECTIBLE`/`dexCount`/`masterCount`/`dexGradeDone`/`recordCorrect`/`MEDALS`/`checkMedals`/`STAGES`/`rankStage`/`exportSave`/`importSave` を **QuizCore へエイリアス**（識別子名は据え置きなので残りのロジックは無改修）。onaji固有（`store.wrong`/`daily`/`unlocked3`/`comebacks`/`dailyTotal`/`soundOn` と 解放チェーン `gradeUnlocked`/`collectableUpTo`/`checkUnlock`、各 render関数）はそのまま。
    - 結果：onaji から **約170行の重複（メダル100＋称号17＋図鑑・記録・セーブ）を削除**（4307行に）。実ブラウザで onaji 全クイズ（⭐⭐⭐ 10/10・totalCorrect/図鑑が QuizCore 経由で加算・メダル7/100・図鑑/メダル/称号描画）・コンソールエラー0 を確認。`validate`/`db-coverage`/CI 合格。**育成系ロジックが1本化**し、図鑑・称号・メダルは onaji↔okurigana で完全合算（同一定義・同一store）。

---

## 5.5 Lark Base 同期

`index.html` の `KANJI`（漢字DB）と `BANK`（問題）を Lark Base（多維表格）へ送る手段。

### 方法A：CSV を手動インポート（認証不要・すぐ）
```bash
cd ~/kanji-quiz && node -e '...'   # ※下記スクリプトで export/ に再生成
```
- 生成物：`~/kanji-quiz/export/kanji.csv`（270字）・`export/questions.csv`（261問）。UTF-8 BOM付き。
- Drive側 `共有ドライブ/family/kanji-quiz-export/` にもコピー済み。
- Lark Base →「インポート」→ CSVアップロードで取り込む。
- ※ `export/` は `.gitignore` 済み（リポジトリには入れない）。

### 方法B：API 自動同期（`lark-sync.mjs`）
`index.html` から読み取り、Lark Base のテーブルを「全削除→最新を一括追加」で置き換える（テーブル/列が無ければ自動作成）。

**準備（1回）**
1. Lark 開発者コンソールで**カスタムアプリ**を用意（既存アプリで可）。Base（多維表格）の**読み書き権限**を付け、公開/承認。
2. 同期先の **Base** を作り、そのアプリを**編集可コラボレーター**として追加。
3. `App ID` / `App Secret` / Base URL の `app_token`（`bascn...`）を控える。
4. `lark.config.sample.json` を `lark.config.json` にコピーして記入（**.gitignore 済み・ローカルのみ**）。
   - `domain`：Lark国際版 `https://open.larksuite.com` / Feishu(中国版) `https://open.feishu.cn`

**実行**
```bash
cd ~/kanji-quiz && node lark-sync.mjs
```
- 作成/更新テーブル：`漢字DB`（漢字・学年・画数・音読み・訓読み）/ `問題`（例文・ID・タイプ・分類・読み・正解・選択肢・選択肢の意味）。
- 何度でも実行可。問題を増やしたら再実行で最新化。`/schedule` で定期実行も可能。
- ⚠ 実機テストは認証情報待ちのため未実施（構文・データ抽出は確認済み）。初回に権限エラーが出たらスコープ名や `domain` を調整する。

### 同期データの定義（列）
- **漢字DB**: 漢字 / 学年(g) / 画数(s) / 音読み(on) / 訓読み(kun)
- **問題**: 例文(sentence, @@が空欄) / ID(BANK index) / タイプ(使い分け・同じ読み) / 分類(tag) / 読み(read) / 正解(answer) / 選択肢(choices) / 選択肢の意味(meaning)

---

## 6. ゼロから公開する手順（再現用メモ）

別マシンや別リポジトリで同じことをする場合の手順。

```bash
# 1. 公開用フォルダを用意（Google Drive内は避ける）
mkdir -p ~/kanji-quiz && cd ~/kanji-quiz
cp /path/to/quiz.html index.html        # URLを綺麗にするため index.html にする

# 2. Gitリポジトリ化して初回コミット
git init -b main
git config user.name  "あなたの名前"
git config user.email "you@example.com"
git add -A && git commit -m "初版"

# 3. GitHub公開リポジトリを作成してアップロード（gh CLI）
gh repo create kanji-quiz --public --source=. --remote=origin --push

# 4. GitHub Pages を有効化（mainのルートを公開）
gh api --method POST /repos/<ユーザー名>/kanji-quiz/pages \
  -f "source[branch]=main" -f "source[path]=/"

# 5. ビルド状況の確認 → URL
gh api /repos/<ユーザー名>/kanji-quiz/pages/builds/latest --jq .status   # built になればOK
gh api /repos/<ユーザー名>/kanji-quiz/pages --jq .html_url
```

> GitHub Pages の無料公開はリポジトリを **public** にする必要がある（本アプリに秘密情報はないので問題なし）。
> ※ ウェブ画面で行う場合: リポジトリ → Settings → Pages → Source を「Deploy from a branch」、Branch を `main` / `/ (root)` に設定。

---

## 7. メモ・今後の選択肢

- **動作確認の自動チェック**: 「答えが選択肢に含まれる／選択肢の漢字がDBにある／全選択肢に意味がある」を Node で検証可能（過去に全問パス）。
- **問題をさらに増やすには**: いまの同じ読みグループに別例文を足すのが手堅い。「使い分け」を大幅に増やすには3年生以上の漢字（暑い/熱い/厚い、開ける、速い/早い 等）の解禁が必要。
- **拡張アイデア**: 2年生160字を全部DB投入／イラスト追加／PWA化（オフライン対応）。（※効果音・音声ON/OFF・1プレイの問題数選択・正解時の画数音訓表示は実装ずみ）
